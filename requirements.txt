fastapi>=0.110
uvicorn>=0.30
pydantic>=2.5
google-genai
anthropic>=0.40
openai>=1.40
python-dotenv
# SaaS-only: required when SAAS_MODE=true. OSS installs can skip — the import
# is lazy inside cloud/supabase_store.py.
supabase>=2.0
